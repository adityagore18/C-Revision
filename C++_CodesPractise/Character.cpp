// code related to Character
#include <iostream>
#include<cctype>

using namespace std;
int main() {
    char c;
    cin>>c;
    
    // c=tolower(c);
    // if(c=='a' || c=='e' || c=='i'||c=='u'||c=='o' ) cout<<"Vowel";
    // else cout<<"Consonant";
    
    // bool isAlpha = isalpha(c);
    // if(isAlpha) cout<<"Character Alphabet";
    // else cout<<"Character is not Alphabet";
   

    return 0;
}